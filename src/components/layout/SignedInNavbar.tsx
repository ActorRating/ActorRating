"use client"

/**
 * Signed-in product pages use the same cinematic navbar as marketing.
 * Kept as a named export so existing SignedInLayout imports stay stable.
 */
export { HomeNavbar as SignedInNavbar } from './HomeNavbar'
